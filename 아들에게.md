# 배포 부탁드립니다 (10분 정도 걸립니다)

초등학교 4학년 국어 수업에서 쓸 웹앱입니다.
학생용 3개 + 교사용 1개, 전부 **의존성 없는 단일 HTML 파일**입니다. 빌드 불필요.

해 주실 일은 세 가지입니다.

---

## ⚠️ 먼저 정해야 할 것 — 구글 계정을 누구 걸로 할지

학생 **이름과 번호**가 시트에 쌓입니다. 학생 개인정보예요.
그래서 **어머니 구글 계정**으로 만드는 게 맞습니다.

세 가지 방법 중 하나를 골라 주세요.

| 방법 | 설명 |
| --- | --- |
| **A. 원격 제어 (권장)** | Chrome 원격 데스크톱이나 AnyDesk로 어머니 PC에 접속해서 대신 조작. 계정·파일 전부 어머니 것으로 남습니다 |
| **B. 어머니 계정으로 로그인** | 어머니가 옆에 계실 때 직접 로그인하시게 하고 그 브라우저에서 작업 |
| **C. 본인 계정으로 진행** | 가장 편하지만 학생 데이터가 본인 구글 드라이브에 쌓입니다. 나중에 시트 소유권을 어머니께 이전해 주세요 |

---

## 1단계. 결과 저장소 만들기 (5분)

1. [sheets.new](https://sheets.new) 에서 새 스프레드시트 생성 → 이름 `우가투스투스 미션 결과`
2. **확장 프로그램 → Apps Script**
3. 기본 `Code.gs` 내용을 **Ctrl+A 로 전부 선택하고** 첨부한 `apps-script/Code.gs` 내용으로 교체
   - ⚠️ 전체 선택 안 하면 기존 `myFunction` 의 `}` 가 남아서 구문 오류 납니다
4. **Ctrl+S** 저장
5. **배포 → 새 배포 → ⚙ → 웹 앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자** ← 학생이 로그인 없이 제출하려면 필수
6. 나온 주소 복사 (`https://script.google.com/macros/s/AKfy....../exec`)

---

## 2단계. 학생용 파일 3개에 주소 넣기 (1분)

`1_말미션_학생용.html`, `2_그림미션_학생용.html`, `3_문자미션_학생용.html`

각 파일을 텍스트 에디터로 열고 위쪽의 이 줄을 찾아서

```js
SHEET_ENDPOINT: "",
```

1단계에서 받은 주소를 넣습니다.

```js
SHEET_ENDPOINT: "https://script.google.com/macros/s/AKfy....../exec",
```

> `teacher-dashboard.html` 은 **수정하지 마세요.** 화면 상단 입력칸에 같은 주소를 붙여넣으면 됩니다.

---

## 3단계. 웹에 올리기 (5분)

파일 4개를 각각 **따로** 올려서 **서로 다른 링크 4개**를 만들어야 합니다.
(학생들을 세 모둠으로 나눠 각각 다른 링크를 주는 수업입니다)

가장 빠른 방법 — **[Netlify Drop](https://app.netlify.com/drop)** 에 파일을 끌어다 놓으면 끝. 로그인도 선택입니다.

Canva Code를 쓰셔도 되는데, `2_그림미션` 이 639 KB라 붙여넣기가 거부될 수 있습니다. 그러면 Netlify로 가세요.

---

## 4단계. 어머니께 보낼 것

링크 4개를 보내 주세요.

```
🗣️ 말 미션    :
🎨 그림 미션  :
📖 문자 미션  :
👩‍🏫 교사용 결과판 :
```

어머니는 결과판 맨 아래 칸에 이 주소들을 넣으시면 QR 코드가 만들어집니다.

---

## 확인 방법

학생용 앱을 열었을 때 **맨 위 노란 경고 띠가 사라져 있으면** 저장소 연결 성공입니다.
경고가 그대로면 주소가 틀렸거나 액세스 권한이 `모든 사용자`가 아닙니다.

한 번 테스트 제출해 보시고, 스프레드시트 `응답` 시트에 줄이 쌓이면 정상입니다.
**테스트한 줄은 지우고** 넘겨 주세요.

---

## 참고

- 자세한 설명은 `설치안내.md` 에 다 있습니다 (수업 진행 순서, 채점 규칙, 문제 해결)
- 내용을 고칠 일은 없겠지만, 고친다면 `src/student.html` 만 수정하고 `build.ps1` 실행
- 저장소를 연결하지 않아도 앱은 전부 동작합니다. 대신 결과가 그 기기에만 남습니다
