# 우가투스투스의 비밀 미션 — 설치 및 수업 안내

초등학교 4학년 국어 「문자가 필요한 까닭을 알아볼까요」 동기유발 웹앱입니다.
선생님이 나눠 주신 수업 자료(안녕 우가투스투스 PPT, 도끼부품 카드, 벽화와 일기 HWPX)의
**실제 내용과 실제 부품 사진·벽화 그림**을 그대로 사용했습니다.

---

## 1. 무엇이 들어 있나요

| 파일 | 용도 | 크기 |
| --- | --- | ---: |
| `1_말미션_학생용.html` | 🗣️ 말로 전달받는 학생용 웹앱 | 213 KB |
| `2_그림미션_학생용.html` | 🎨 벽화만 보고 하는 학생용 웹앱 | 641 KB |
| `3_문자미션_학생용.html` | 📖 일기를 읽고 하는 학생용 웹앱 | 213 KB |
| `teacher-dashboard.html` | 👩‍🏫 교사용 실시간 결과판 | 26 KB |
| `apps-script/Code.gs` | 결과를 Google Sheet에 저장하는 코드 | — |
| `src/student.html`, `build.ps1`, `assets/` | 학생용 3종을 다시 만들 때 쓰는 원본 | — |

네 개 파일은 모두 **그 자체로 완결된 HTML 한 장**입니다.
더블클릭해서 바로 열어도 되고, Canva Code에 붙여넣어도 되고, 어떤 웹 호스팅에 올려도 됩니다.

---

## 2. 결과 저장 연결하기 (약 5분, 한 번만)

> **먼저 알아 두실 점**
> Canva Code의 "연결된 Canva Sheet"는 Canva 편집 화면에서 **보여 줄 데이터를 붙이는** 기능이고,
> 게시된 앱이 학생 응답을 **거꾸로 기록해 넣는** 공개 기능은 제공되지 않습니다.
> 그래서 서로 다른 3개 링크의 결과를 한 곳에 모으려면 별도 저장소가 필요합니다.
> 아래 Google Sheet 방식은 무료이고, 실제로 기기와 상관없이 결과가 모입니다.

### ① 스프레드시트 만들기
1. [sheets.new](https://sheets.new) 로 새 스프레드시트를 만듭니다.
2. 이름을 예: `우가투스투스 미션 결과` 로 바꿉니다.

### ② 스크립트 붙여넣기
3. 상단 메뉴 **확장 프로그램 → Apps Script** 를 엽니다.
4. 기본으로 있는 `Code.gs` 내용을 모두 지우고,
   이 폴더의 `apps-script/Code.gs` 내용을 **전부 복사해 붙여넣습니다.**
5. **저장(💾 또는 `Ctrl+S`)합니다. ← 꼭 먼저 저장하세요.**
   저장하기 전에는 위쪽 함수 목록이 `myFunction` 그대로이고, 아래 6번의 함수가 보이지 않습니다.
6. *(선택)* 함수 목록에서 **`setup`** 을 고르고 **실행**을 누릅니다.
   - 권한 승인 창이 뜨면 → `고급` → `...(안전하지 않음)으로 이동` → `허용`
   - 스프레드시트에 **응답** 시트와 머리글이 만들어집니다.
   - **건너뛰어도 됩니다.** 시트는 첫 제출 때 자동으로 만들어지고,
     권한 승인은 다음 단계인 배포에서도 뜹니다.

### ③ 웹 앱으로 배포하기
7. 오른쪽 위 **배포 → 새 배포** 를 누릅니다.
8. 톱니바퀴 ⚙ → **웹 앱** 을 고릅니다.
9. 아래와 같이 설정합니다.
   - **실행 계정**: 나
   - **액세스 권한**: **모든 사용자** ← 학생이 로그인 없이 제출하려면 꼭 이렇게 해야 합니다.
10. **배포** 를 누르면 나오는 주소를 복사합니다.
    `https://script.google.com/macros/s/AKfy....../exec`

### ④ 웹앱에 주소 넣기
11. 학생용 HTML 파일 3개를 메모장(또는 Canva Code 편집기)으로 열고,
    위쪽에 있는 이 한 줄을 찾아 주소를 붙여넣습니다.

    ```js
    SHEET_ENDPOINT: "",
    ```
    ⬇️
    ```js
    SHEET_ENDPOINT: "https://script.google.com/macros/s/AKfy....../exec",
    ```

    바로 아래 `CLASS_LABEL` 에 `"4학년 1반"` 처럼 반 이름을 넣어도 됩니다(선택).

12. `teacher-dashboard.html` 은 고칠 필요가 없습니다.
    결과판 화면 맨 위 칸에 **같은 주소**를 붙여넣고 `[연결하고 불러오기]` 를 누르면 됩니다.
    (그 기기에 저장되어 다음에 열 때 자동으로 불러옵니다.)

### 잘 됐는지 확인
학생용 앱을 열었을 때 맨 위의 **노란 경고 띠가 사라졌다면** 연결된 것입니다.
경고가 그대로면 주소가 잘못되었거나 액세스 권한이 `모든 사용자`가 아닙니다.

---

## 3. Canva Code에 올리고 링크 만들기

학생용 3개는 **각각 따로** 만들어야 서로 다른 링크가 나옵니다.

1. Canva에서 **Canva Code** 프로젝트를 새로 만듭니다.
2. `1_말미션_학생용.html` 의 내용을 **전부** 붙여넣습니다.
3. 게시(Publish)하고 링크를 복사합니다.
4. 2번, 3번 파일도 **각각 새 프로젝트**로 반복합니다.
5. `teacher-dashboard.html` 도 같은 방식으로 하나 더 만듭니다.
6. 결과판 맨 아래 **학생 배포용 링크** 칸에 네 개 주소를 넣으면
   링크 복사 버튼과 **QR 코드**가 만들어집니다.

> **붙여넣기가 너무 크다고 나오면**
> `2_그림미션_학생용.html` 은 벽화 그림 4장이 들어 있어 641 KB입니다.
> Canva Code가 거부하면, 이 HTML 파일을 그대로
> Netlify Drop·Google Sites·학교 홈페이지 등 아무 곳에나 올려도 똑같이 동작합니다.
> 파일 하나로 완결되어 있어 따로 설치할 것이 없습니다.

---

## 4. 저장 없이 그냥 써 보기

Google Sheet를 설정하지 않아도 **모든 기능이 그대로 동작**합니다.
다만 결과가 그 기기에만 남고 교사용 결과판에 모이지 않으며,
학생 화면 맨 위에 그 사실이 정직하게 표시됩니다.
수업 전에 혼자 미리 해 보실 때는 이대로 쓰셔도 됩니다.

---

## 5. 수업 진행 순서

1. 학급을 세 모둠으로 나누고 **서로 다른 링크**를 나눠 줍니다.
   - 🗣️ 말 모둠 / 🎨 그림 모둠 / 📖 문자 모둠
2. 학생은 이름·번호를 넣고 각자 방식으로 제작 방법을 전달받습니다.
3. 재료를 골라 제출하면 **즉시 점수와 성공 여부**가 나옵니다.
4. 학생은 "가장 어려웠던 점"을 골라 보냅니다.
5. 선생님은 결과판을 전자칠판에 띄웁니다.
   - `[자동 새로고침 켬]` 을 누르면 30초마다 저절로 갱신됩니다.
6. **「세 가지 전달 방법, 결과가 어떻게 달랐을까?」** 카드로 세 미션 평균을 함께 봅니다.
7. 아래 질문 카드로 본 차시로 넘어갑니다.
   - "왜 같은 미션인데 점수가 달랐을까요?"
   - "문자가 필요한 까닭은 무엇일까요?"

> 이 웹앱은 **문자가 좋다는 결론을 먼저 알려 주지 않습니다.**
> 학생이 직접 겪은 점수 차이에서 스스로 발견하도록 설계했습니다.

### 비교가 성립하도록 지킨 것

| 미션 | 학생이 받는 자료 | 문자 포함 여부 |
| --- | --- | --- |
| 🗣️ 말 | 음성으로만 10단계. 화면에는 "몇 번째 이야기"와 말하는 표시만 | **없음** |
| 🎨 그림 | 동굴 벽화 4장. 원본에 있던 영어 글자는 **지웠습니다** | **없음** |
| 📖 문자 | 일기 전문. 재료를 고르는 중에도 다시 읽기 가능 | 있음 (이 미션의 조건) |

- 말 미션은 **지나간 이야기를 다시 들을 수 없습니다.** 현재 이야기만 다시 들을 수 있습니다.
- 그림 미션은 **재료 선택 화면으로 넘어가면 벽화를 다시 볼 수 없습니다.**
- 문자 미션만 언제든 다시 확인할 수 있습니다. 이 차이가 점수 차이를 만듭니다.

> **재료 카드의 이름표에 대하여**
> 재료 카드에는 "이끼 낀 푸른 뾰족돌" 같은 짧은 이름표가 붙어 있습니다.
> 이것은 **세 미션 모두 똑같이** 보이는 정답 선택 화면이고, 만드는 방법(레시피)은
> 알려 주지 않으므로 특정 미션이 유리해지지 않습니다.
> 4학년 학생이 사진만으로 미세한 차이를 구분하기 어려워 넣었습니다.
> 떼고 싶으시면 `src/student.html` 의 `CARDS` 에서 `label` 값을 비우면 됩니다.

---

## 6. 채점 규칙과 정답

총점 100점 · 정답 4개 각 25점 · **오답 1개당 −5점** · 최저 0점

| 부품 | 정답 |
| --- | --- |
| 망치 머리 | 이끼가 낀 **푸른색**의 **뾰족한** 돌 |
| 손잡이 | **불에 타고 이끼는 없는 흰색 길쭉한** 나무 |
| 넝쿨 | **가시는 없고 이파리만** 있는 넝쿨 |
| 장신구 | **이파리는 없고 가시만 있는 흰색 네모난** 장신구 |

| 점수 | 표시 |
| ---: | --- |
| 90–100 | 🎉 미션 성공! |
| 70–89 | 👍 거의 성공! |
| 50–69 | 🤔 다시 생각해 봐요! |
| 0–49 | 🔎 우가투스투스의 정보를 다시 확인해 봐요! |

재료 카드는 오답 16종을 포함해 모두 20장이며, 원본 도끼부품 카드 이미지를 잘라서 썼습니다.

---

## 7. 결과판 읽는 법

- **집계 기준**을 `최고 점수 / 첫 시도 / 마지막 시도` 중에서 고를 수 있습니다.
  재도전을 허용했다면 **첫 시도** 기준이 세 방법의 차이를 가장 잘 보여 줍니다.
- 학생 한 명은 미션마다 한 줄로 표시됩니다.
- 제출이 없으면 숫자를 지어내지 않고 **"아직 제출한 학생이 없습니다"** 라고 표시합니다.
- 표의 제목을 누르면 정렬되고, 미션별로 걸러 볼 수 있습니다.

---

## 8. 문제가 생기면

| 증상 | 확인할 것 |
| --- | --- |
| 노란 경고가 계속 보인다 | `SHEET_ENDPOINT` 주소가 `/exec` 로 끝나는지, 액세스 권한이 `모든 사용자`인지 |
| "결과를 저장하는 중 문제가 발생했습니다" | 인터넷 연결, 그리고 위와 같은 배포 설정. 학생은 다시 제출하면 됩니다 |
| 결과판이 비어 있다 | 결과판에 넣은 주소가 학생용과 **똑같은지** |
| 말 미션에서 소리가 안 난다 | 먼저 기기 음량을 확인하세요. 음성 기능 자체가 없는 기기라면 화면에 **[🧑‍🏫 선생님이 읽어 주기]** 버튼이 나타납니다. 또는 아래 11번의 대본을 선생님이 읽어 주세요 |
| QR이 안 만들어진다 | 학교망이 외부 스크립트를 막은 경우입니다. 링크 복사 버튼은 그대로 됩니다 |
| 결과판에 "이 기기에 남아 있는 기록만" 이라고 뜬다 | 저장소에 연결되지 않은 상태입니다. 다른 학생 결과는 포함되지 않습니다 |
| Apps Script 함수 목록에 `setup`이 안 보인다 | 아직 저장하지 않은 것입니다. `Ctrl+S` 로 저장하면 목록이 새로 채워집니다 |

---

## 9. 개인정보

- 수집: **학생 이름, 번호** 만.
- 수집하지 않음: 전화번호, 이메일, 주소, 생년월일, 비밀번호.
- 결과는 선생님 소유의 Google Sheet에만 저장됩니다.
- 수업이 끝나면 응답 시트를 정리해 주세요.

---

## 10. 내용을 고치고 싶다면

`src/student.html` 한 파일만 고치고 PowerShell에서 아래를 실행하면
학생용 3종이 다시 만들어집니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

- 일기 원문: `DIARY`
- 말 미션 설명 10단계: `SPEECH_STEPS`
- 재료 카드와 정답: `CARDS` (`ok:true` 가 정답)
- 어려웠던 점 선택지: `REFLECT_OPTIONS`

---

## 11. 말 미션 대본 (선생님용)

기기에서 소리가 나지 않을 때 선생님이 직접 읽어 주실 수 있도록 옮겨 둡니다.
**학생 화면에는 이 문장이 글자로 나오지 않습니다.** 한 문장씩 읽고 [다음]을 누르게 하세요.

1. 가장 먼저, 푸른색 돌을 찾아라. 여러 돌을 비교했지만 푸른색 돌이 가장 단단하다.
2. 그 돌을 숲 속에 놓아 이끼가 자랄 때까지 기다려라. 이끼가 돌 틈새를 메워 더 튼튼해진다.
3. 이끼가 자랐다면, 돌을 뾰족한 모양으로 깎아라. 뾰족하면 뾰족할수록 좋다.
4. 다음은 손잡이다. 흰색 나무를 찾아라.
5. 손잡이가 너무 짧으면 불편하니, 길게 깎아야 한다.
6. 그 다음, 손잡이를 불에 살짝 구워라. 불에 그을리면서 나무가 더 딱딱해진다.
7. 이제 돌과 손잡이를 엮을 넝쿨이 필요하다. 가시와 이파리가 달린 넝쿨을 구해라.
8. 넝쿨의 가시는 모두 떼어내라. 이파리만 달린 넝쿨로 머리와 손잡이를 엮어라.
9. 마지막은 장신구다. 가시가 달린 흰색 열매를 구해 네모 모양으로 깎아라.
10. 열매의 이파리는 모두 제거해라. 흰색의 가시 달린 네모난 장신구가 가장 좋다.
